"""Evaluation package — the test harness that proves the system works.

Runs 20+ synthetic incidents through the agent and computes the portfolio
metrics table (PRD §6.4).

Contents (implemented in Phase 4 — Evaluation & Documentation):
    run_eval.py  -- runs all incidents, collects results   (WI-35)
    metrics.py   -- resolution rate, escalation precision/recall,
                    failure recovery rate, audit completeness,
                    DLQ capture rate, avg steps to resolution (WI-36)

Status: PLACEHOLDER — see docs/PHASES.md (Phase 4, WI-34..WI-38).
"""
